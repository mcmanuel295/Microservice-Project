spring:
  application:
    name: config-server

  profiles:
    active: native
  cloud:
    config:
      enabled: false
      import-check:
        enabled: false
      server:
        native:
          search-locations: classpath:/configs/

