spring:
  application:
    name: school-service
    config:
      students-url: http://student-service/students
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    url: jdbc:mysql://localhost:3307/msnetwork
    password: 123


  jpa:
    hibernate.ddl-auto:  update
    show-sql: true

server:
  port: 8081


eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

